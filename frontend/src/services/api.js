// src/services/api.js
// DEMO MODE — no backend needed

const api = {
    get: async (url) => ({ data: {} }),
    post: async (url, data) => ({ data: {} }),
    put: async (url, data) => ({ data: {} }),
    delete: async (url) => ({ data: {} }),
};

export default api;